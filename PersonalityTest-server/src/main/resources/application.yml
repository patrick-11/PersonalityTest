spring:
  application:
    name: "Personality Test"
  server:
    port: 8080
  datasource:
    url: "jdbc:postgresql://localhost:5433/personalitytest"
    username: "postgres"
    password: "postgres"
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: "org.hibernate.dialect.PostgreSQLDialect"


management:
  security:
    enabled: false
  port: 9000
